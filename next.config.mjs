/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["images.generated.photos", "img.freepik.com"], // Add the domain for external images
  },
};

export default nextConfig;
